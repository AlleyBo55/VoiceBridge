# VoiceBridge — Environment Variables
# Copy this file to .env and fill in your values.
# Never commit .env to git.

# ── Build ────────────────────────────────────────────────────
VITE_VERSION=1.0.0

# ── Notes ────────────────────────────────────────────────────
# API keys are entered by the user during onboarding (first launch)
# and stored encrypted (AES-GCM-256) on disk. No keys are embedded
# in the build. Users can change keys anytime in Settings.
